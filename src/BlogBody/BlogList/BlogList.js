/* eslint-disable no-unused-vars */
import React, { useState } from 'react';
import BlogPost from './BlogPost';

/** {component} List @경로 App/BlogList */
export default function BlogList(list) {
  // state

  return <div className='blogList'></div>;
}
